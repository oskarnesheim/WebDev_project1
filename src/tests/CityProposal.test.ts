import CityProposal from "../components/CityProposal";
import { render, screen, cleanUp } from "@testing-library/react";
import { describe, it, expect } from "vitest";

describe("CityProposal", () => {});
